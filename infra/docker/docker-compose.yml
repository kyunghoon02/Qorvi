services:
  postgres:
    image: postgres:16-alpine
    container_name: whalegraph-postgres
    environment:
      POSTGRES_DB: whalegraph
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - whalegraph_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d whalegraph"]
      interval: 5s
      timeout: 3s
      retries: 20

  neo4j:
    image: neo4j:5.24-community
    container_name: whalegraph-neo4j
    environment:
      NEO4J_AUTH: neo4j/neo4jpassword
      NEO4J_server_memory_pagecache_size: 512M
      NEO4J_server_memory_heap_initial__size: 512M
      NEO4J_server_memory_heap_max__size: 512M
      NEO4J_PLUGINS: '["apoc"]'
    ports:
      - "7474:7474"
      - "7687:7687"
    volumes:
      - whalegraph_neo4j_data:/data
      - whalegraph_neo4j_logs:/logs
    healthcheck:
      test:
        - CMD-SHELL
        - cypher-shell -u neo4j -p neo4jpassword 'RETURN 1' >/dev/null 2>&1 || exit 1
      interval: 10s
      timeout: 5s
      retries: 30

  redis:
    image: redis:7.4-alpine
    container_name: whalegraph-redis
    command: ["redis-server", "--appendonly", "yes"]
    ports:
      - "6379:6379"
    volumes:
      - whalegraph_redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 20

volumes:
  whalegraph_postgres_data:
  whalegraph_neo4j_data:
  whalegraph_neo4j_logs:
  whalegraph_redis_data:
